server:
  port: 8081

spring:
  application:
    name: gateway-service

  data:
    redis:
      host: localhost
      port: 6379

  threads:
    virtual:
      enabled: true

jwt:
  secret: "${JWT_SECRET}"