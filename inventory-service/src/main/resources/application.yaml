server:
  port: 8082

spring:
  application:
    name: inventory-service

  # PostgreSQL Connection stays! We still want relational queries and locks.
  datasource:
    url: jdbc:postgresql://localhost:5432/surge_db
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true